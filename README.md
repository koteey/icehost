# Icers v4.0 - Юзербот для VK

![Version](https://img.shields.io/badge/version-4.0-blue)


**Мощный юзербот для VK с продвинутыми возможностями обработки медиа, системой модулей и ролевым доступом. Полная работоспособность не гарантируется.**

Разработано командой **SnowCode** ([@snowcoding](https://t.me/snowcoding))

---

## 📋 Оглавление

- [🌟 Особенности](#-особенности)
- [⚡ Быстрый старт](#-быстрый-старт)
- [📦 Установка](#-установка)
- [⚙️ Настройка](#️-настройка)
- [🎮 Команды](#-команды)
- [🧩 Модули](#-модули)
- [🔐 Система доступа](#-система-доступа)
- [🔧 Для разработчиков](#-для-разработчиков)
- [🚀 Возможности](#-возможности)
- [⚠️ Безопасность](#️-безопасность)
- [📞 Поддержка](#-поддержка)

---

## 🌟 Особенности

### 🎨 **Медиа-обработка**
- **Фото**: фильтры, демотиваторы, текст, жмых (seam carving)
- **Аудио**: изменение тона/скорости/громкости, конвертация
- **Видео**: базовые операции
- **QR-коды**: генерация и сканирование

### 🤖 **Интеллектуальные функции**
- **Система модулей** с hot-reload
- **Ролевая система доступа** (уровни 0-4)
- **Автоответы** на сообщения
- **Обработка событий** VK LongPoll

### ⚡ **Производительность**
- **Асинхронная обработка**
- **Кэширование временных файлов**
- **Оптимизация памяти**
- **Цветное логирование**

---

## ⚡ Быстрый старт

1. **Скачайте релиз** с GitHub
2. **Установите зависимости**: `pip install vk-api requests pillow pydub numpy qrcode`
3. **Настройте** `icersdata.ini`
4. **Запустите**: `python icers.py`
5. **Используйте** команды с префиксом `.`

---

## 📦 Установка

### Требования
- Python 3.7 или выше
- Токен доступа VK (получите на [vkhost.github.io](https://vkhost.github.io/))
- Ваш user_id VK

### Шаг 1: Установка зависимостей

```bash
# Базовые зависимости
pip install vk-api requests pillow pydub

# Опциональные (для расширенных функций)
pip install numpy qrcode pyzbar
```

### Шаг 2: FFmpeg (для аудиообработки)
```bash
# Windows: скачайте с ffmpeg.org и распакуйте в папку ffmpeg/
# Linux: sudo apt-get install ffmpeg
# Mac: brew install ffmpeg
```

---

## ⚙️ Настройка

### Файл `icersdata.ini`
```ini
[VK]
token = ваш_токен_доступа_здесь
user_id = ваш_user_id_здесь
```

### Получение токена:
1. Перейдите на [vkhost.github.io](https://vkhost.github.io/)
2. Выберите **Kate Mobile**
3. Разрешите доступ
4. Скопируйте токен из адресной строки (параметр `access_token`)

### Получение user_id:
1. Откройте ваш профиль VK
2. URL будет: `https://vk.com/id123456789`
3. Цифры после `id/` - ваш user_id

---

## 🎮 Команды

### 📱 **Медиа и изображения**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.copy` | Копировать сообщение с вложениями | `.copy` (ответ на сообщение) |
| `.negative` / `.n` | Инвертировать фото | `.negative` (ответ на фото) |
| `.demot <текст>` | Создать демотиватор | `.demot Я устал` |
| `.text <1/2> <текст>` | Добавить текст на фото | `.text 1 Привет` |
| `.dist` / `.d [1-10]` | Жмых изображения | `.dist 5` |
| `.meme` / `.мем <верх>\|<низ>` | Создать мем | `.meme ПРОГРАММИСТ\|КОГДА НАХОДИТ БАГ` |
| `.filter <фильтр>` | Фильтры для фото | `.filter grayscale` |

### 🔊 **Аудио и голосовые**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.boost` / `.bu <множитель>` | Усилить аудио (1-5000x) | `.boost 10` |
| `.pitch <множитель>` | Изменить тон (0.5-2.0) | `.pitch 1.5` |
| `.speed <множитель>` | Изменить скорость (0.5-3.0) | `.speed 2.0` |

### 🎮 **Развлечения и игры**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.random <список>` | Случайный выбор | `.random пицца,суши,бургер` |
| `.dice` | Бросить кубик (1-6) | `.dice` |
| `.coin` | Подбросить монетку | `.coin` |
| `.chance <событие>` | Шанс события | `.chance выиграть в лотерею` |
| `.kto` / `.кто <вопрос>` | Случайный выбор пользователя | `.kto лучший друг` |
| `.vos` / `.шар <вопрос>` | Шар предсказаний | `.vos Стоит ли мне учить Python?` |
| `.spam <кол-во> <задержка> <текст>` | Спам сообщениями | `.spam 5 1 Привет!` |

### 📱 **QR-коды**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.qr <текст>` | Создать QR-код | `.qr https://vk.com` |
| `.qrscan` | Прочитать QR-код | `.qrscan` (ответ на фото с QR) |

### 📊 **Бекапы**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.backupall` | Полный бекап бота | `.backupall` |
| `.restoreall` | Восстановить из бекапа | `.restoreall` (ответ на файл .py) |

### 👤 **Профиль**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.davatar` | Аватарка в избранное | `.davatar` (ответ на пользователя) |
| `.accountinfo` | Информация об аккаунте | `.accountinfo` |
| `.setname <имя> <фамилия>` | Изменить имя | `.setname Иван Иванов` |
| `.setphoto` | Установить аватар | `.setphoto` (ответ на фото) |
| `.post <текст>` | Опубликовать запись | `.post Привет мир!` |

### ⚙️ **Системные**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.info` | Информация о боте | `.info` |
| `.ping` | Проверка соединения | `.ping` |
| `.restart` | Перезапустить бота | `.restart` |
| `.settings` | Управление настройками | `.settings` |
| `.terminal <команда>` | Выполнить в терминале | `.terminal dir` |
| `.python <код>` | Выполнить Python код | `.python print("Hello")` |
| `.vkapi <метод> <параметры>` | Вызов VK API | `.vkapi users.get user_ids=1` |

### 🔐 **Управление доступом**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.ubac <уровень> <пользователь>` | Управление доступом | `.ubac 3 @id123456` |

**Уровни доступа:**
- `0` - ❌ Нет доступа
- `1` - ✅ Полный доступ (все команды)
- `2` - 🔧 Только команды (кроме управления)
- `3` - 🎮 Только развлечения
- `<имя_модуля>` - 📦 Доступ только к модулю

### 🧩 **Модули**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.modules` | Список модулей | `.modules` |
| `.dm` | Установить модуль | `.dm` (ответ на .py файл) |
| `.delm <имя>` | Удалить модуль | `.delm test_module` |

### 🔑 **Хоткеи**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.hotkey <алиас> <команда>` | Создать хоткей | `.hotkey пинг ping` |
| `.hotlist` | Список хоткеев | `.hotlist` |
| `.delhotkey <алиас>` | Удалить хоткей | `.delhotkey пинг` |

### ❓ **Помощь**
| Команда | Описание | Пример |
|---------|----------|--------|
| `.help` | Полная справка | `.help` |
| `.911` | Быстрая помощь | `.911` |
| `.112` | Быстрая помощь | `.112` |
| `.sos` | Быстрая помощь | `.sos` |

---

## 🧩 Модули

### Установка модулей

#### Способ 1: Через файл
1. Создайте файл `modules/ваш_модуль.py`
2. Напишите код по шаблону ниже
3. Бот автоматически загрузит модуль при запуске

#### Способ 2: Через сообщение
1. Отправьте файл `.py` в диалог с ботом
2. Ответьте на файл командой `.dm`
3. Модуль установится автоматически

### Шаблон модуля

```python
MODULE_INFO = {
    'name': 'Название модуля',
    'version': '1.0',
    'description': 'Описание модуля',
    'author': 'Ваше имя'
}

MODULE_COMMANDS = [
    'test - тестовая команда'
]

def process_command(command, vk, peer_id, user_id, settings=None):
    """Обработка команд с префиксом"""
    if command == 'test':
        return "✅ Тест выполнен!"
    return None

# ОПЦИОНАЛЬНО: дополнительные обработчики

def on_message_received(message_text, vk, peer_id, message_id, from_user_id, user_id, settings):
    """Обработка ВСЕХ сообщений"""
    if "привет" in message_text.lower():
        vk.messages.send(peer_id=peer_id, message="И тебе привет!", random_id=0)
    return None

def on_any_message(message_text, vk, peer_id, message_id, from_user_id, user_id, settings):
    """Обработка сообщений от ЛЮБОГО пользователя"""
    if from_user_id != user_id:
        if "бот" in message_text.lower():
            vk.messages.send(peer_id=peer_id, message="Я здесь! 🎯", random_id=0)
    return None
```

### Доступные утилиты для модулей

```python
def process_command(command, vk, peer_id, user_id, settings=None):
    utils = settings.get('_utils', {})
    
    # Основные утилиты
    uptime = utils.get('get_uptime', lambda: "Недоступно")()
    latency = utils.get('measure_network_latency', lambda vk: 0)(vk)
    
    # Файловые операции
    utils.get('download_file', lambda url, fname: False)("url", "file.txt")
    
    # Логирование
    utils.get('module_log', print)("MyModule", "Сообщение")
    
    return f"⏱️ Аптайм: {uptime}"
```

---

## 🔐 Система доступа

### Уровни доступа

#### Уровень 0: ❌ Нет доступа
- Только владелец может использовать команды

#### Уровень 1: ✅ Полный доступ
- Все команды и модули
- Управление доступом
- Системные команды

#### Уровень 2: 🔧 Только команды
- Все команды кроме:
  - `.ubac` (управление доступом)
  - `.terminal` (терминал)
  - `.python` (Python код)
  - `.vkapi` (VK API)
  - `.restart` (перезапуск)

#### Уровень 3: 🎮 Только развлечения
- Медиа команды (`.copy`, `.demot`, `.meme`, `.filter`)
- Аудио команды (`.boost`, `.pitch`, `.speed`)
- Развлечения (`.random`, `.dice`, `.coin`, `.chance`, `.kto`, `.vos`)
- QR-коды (`.qr`, `.qrscan`)
- Спам (`.spam`)

#### Уровень "module": 📦 Доступ к модулю
- Только указанные модули
- Без доступа к системным командам

### Примеры использования

```bash
# Дать полный доступ пользователю
.ubac 1 @id123456

# Дать доступ только к развлечениям
.ubac 3 @id123456

# Дать доступ к конкретному модулю
.ubac my_module @id123456

# Отобрать все права
.ubac 0 @id123456

# Показать текущие права
.ubac
```

---

## 🔧 Для разработчиков

### Структура проекта

```
icers/
├── icers.py                    # Главный файл
├── icersdata.ini              # Конфигурация
├── settings.json              # Настройки
├── hotkeys.json               # Хоткеи
├── user_access.json           # Права доступа
├── commands/                  # Папка с командами
│   ├── __init__.py           # Маршрутизатор команд
│   ├── info.py               # Команда .info
│   ├── ping.py               # Команда .ping
│   └── ...                   # 35+ других команд
├── lib/                       # Библиотеки
│   ├── __init__.py           # Экспорт библиотек
│   ├── config.py             # Загрузка конфигурации
│   ├── settings.py           # Работа с настройками
│   ├── modules.py            # Система модулей
│   ├── logger.py             # Цветное логирование
│   ├── image_utils.py        # Обработка изображений
│   ├── audio_utils.py        # Обработка аудио
│   ├── media_utils.py        # Работа с медиа
│   ├── system_utils.py       # Системные утилиты
│   ├── vk_utils.py           # Утилиты VK API
│   └── backup_utils.py       # Система бекапов
└── modules/                  # Папка для пользовательских модулей
```

### Архитектура

1. **Главный цикл** (`icers.py`):
   - Обработка LongPoll событий VK
   - Маршрутизация сообщений
   - Логирование

2. **Командный процессор** (`commands/__init__.py`):
   - Определение команды
   - Вызов соответствующего обработчика
   - Обработка результатов

3. **Система модулей** (`lib/modules.py`):
   - Динамическая загрузка модулей
   - Проверка безопасности
   - Изоляция среды выполнения

### API для модулей

#### Доступные функции:
```python
# Через settings['_utils']
utils = settings.get('_utils', {})

# Системные
utils['get_uptime']()                    # Время работы бота
utils['measure_network_latency'](vk)     # Задержка до VK
utils['get_connection_quality'](latency) # Качество соединения

# Файловые
utils['download_file'](url, filename)    # Скачивание файла

# Настройки
utils['save_settings'](settings)         # Сохранение настроек
utils['load_settings']()                 # Загрузка настроек

# Логирование
utils['module_log'](name, message)       # Логирование модуля
```

---

## 🚀 Возможности

### Медиа-обработка
- **Seam carving** с растягиванием (оптимизированный алгоритм)
- **Демотиваторы** с кастомным текстом и рамками
- **Фильтры** (сепия, инверсия, размытие, резкость)
- **Текст на фото** с настраиваемым шрифтом и позицией

### Аудио-обработка
- **Изменение тона** (0.5x - 2.0x)
- **Изменение скорости** (0.5x - 3.0x)
- **Усиление громкости** (1x - 5000x)
- **Конвертация форматов** (ogg ↔ mp3)

### Интеграции
- **Прямой доступ к VK API** через `.vkapi`
- **Выполнение Python кода** через `.python`
- **Терминальные команды** через `.terminal`
- **QR-коды** с генерацией и сканированием

### Безопасность
- **Проверка модулей** на опасный код
- **Ограничение доступа** модулей к системе
- **Ролевая система** с гибкими правами
- **Изоляция** пользовательского кода

---

## ⚠️ Безопасность

### Важные предупреждения

1. **Токен VK** — это ключ доступа к вашему аккаунту:
   - Никогда не передавайте его третьим лицам
   - Используйте только с доверенными ботами
   - Регулярно проверяйте активные сессии

2. **Команды с повышенными правами**:
   - `.terminal` — выполнение любых команд системы
   - `.python` — выполнение любого Python кода
   - `.vkapi` — вызов любых методов VK API

3. **Модули пользователей**:
   - Проверяйте код перед установкой
   - Устанавливайте только из доверенных источников
   - Используйте систему доступа для ограничения

### Рекомендации

1. **Используйте отдельный аккаунт** для бота
2. **Не давайте уровень 1** (полный доступ) посторонним

---

## 📞 Поддержка

### Полезные ссылки
- **GitHub репозиторий**: [github.com/snowcoding/icers](https://github.com/koteey/icers)
- **Автор**: SnowCode ([@snowcoding](https://t.me/snowcoding))
- **Версия**: 4.0

---

**Icers v4.0** — мощный инструмент для автоматизации VK с открытым исходным кодом.  
Используйте с умом и создавайте удивительные вещи! ✨

---

*Разработано с ❤️ командой SnowCode*


